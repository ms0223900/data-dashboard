---
description: PROTOTYPE cross-cutting TS style—speed first; non-negotiable validation, secrets, and user-facing zh-TW errors.
alwaysApply: true
---

# Code Style（PROTOTYPE）

## 本模式態度

- **先跑 spec、再優化**。可接受較扁平、較少的檔案切分。
- **`any`**：盡量少用；若省下大量時間可先寫並加 **`TODO(prototype): narrow type`**。

## 絕對底線（與 [`docs/spec.md`](docs/spec.md)、`AGENTS.md` 一致）

- **驗證**：表單／Route Handler / Server Action 邊界驗證 `email` 及必填欄位；**無效資料不寫入 Supabase**。
- **錯誤文案（繁中）**：Email 空白或格式錯誤訊息與 spec 一致或共用常數。
- **外部 I/O**：避免空 `catch`；API 或資料庫失敗時至少讓 UI 顯示可讀訊息，不要整頁崩潰。
- **機密**：同 `AGENTS.md`—不得把 service role 或敏感 credentials 暴露到 client。

## 可妥協項目

- 未抽共用 helper、暫不重複程式碼收斂。
- 不深究每個分支的詳盡錯誤型別命名；但 **critical path**（寫 DB、寫事件）仍要有明確成敗分支。

## Do / Don't

| Do | Don't |
| --- | --- |
| 對照 spec 驗證／錯誤字串 | 略過驗證寫入 `leads` |
| 失敗時繁中、可重試 | 靜默失敗 |
| 機密僅 server | `NEXT_PUBLIC_*` 洩漏 service role |

## 參照

- [`AGENTS.md`](AGENTS.md)、[`docs/spec.md`](docs/spec.md)
