---
description: PROTOTYPE Next.js—inline fetch/db write OK in early delivery; server-only secrets; no complex API layer initially.
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Next.js（PROTOTYPE）

## 目標

- **快**：先讓 Landing Page、表單與 API 可驗收；可暫時在 route 或單一檔內寫 fetch/DB write。

## 不可妥協

- **`NEXT_PUBLIC_*`** 不得放 service role 或敏感 API Key。
- 需要 token 或特權的邏輯只在 **server**（Route Handler / Server Action）。

## App Router

- 需要 `useState`、瀏覽器 API、狀態切換 → `'use client'`。
- Route Handler：**盡量**回一致 JSON／HTTP status；來不及時也需 **不洩 secret**。
- Tailwind：**可**將樣式寫在同一元件檔；另拆 CSS module 非必須。

## Do / Don't

| Do | Don't |
| --- | --- |
| 機密與 mutations 留 server | client 直接用 service role 寫庫 |
| API 失敗有繁中提示 | 白屏／uncaught crash |
| 重複邏輯第三次再抽 | 一開始就過度分層 |

## 參照

- [`docs/spec.md`](docs/spec.md)、[`AGENTS.md`](AGENTS.md)
